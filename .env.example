# Public variables — shown on the dashboard and returned by /api/env
APP_NAME=ServerCompass Meteor Demo
API_URL=https://api.servercompass.app
ENVIRONMENT=production
VERSION=1.0.0

# Private variables — loaded server-side only, never exposed to the browser
DATABASE_URL=mongodb://localhost:27017/servercompass
API_SECRET_KEY=your-secret-key-here

# Meteor runtime config
ROOT_URL=http://localhost:3000
PORT=3000
