{
  "name": "servercompass-meteor-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build": "meteor build --directory /tmp/meteor-bundle --server-only",
    "lint": "echo \"No linter configured\""
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.ts"
    }
  },
  "dependencies": {
    "@babel/runtime": "^7.26.0",
    "@types/node": "^22.0.0",
    "meteor-node-stubs": "^1.2.9"
  }
}
